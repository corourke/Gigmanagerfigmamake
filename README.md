
  # GigManager

  This is a code bundle for GigManager. The original project is available at https://www.figma.com/design/Kujdbw7w9IEPUUlH1GCIaE/GigManager.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  